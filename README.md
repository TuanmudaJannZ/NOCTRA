# /assets

Letakkan file MP3 kamu di sini, atau gunakan fitur drag & drop langsung ke browser.

File MP3 yang diletakkan di folder ini tidak akan otomatis terdeteksi karena
browser memerlukan interaksi pengguna untuk memuat file lokal.

Gunakan tombol "Import Music" atau drag & drop file MP3 ke jendela browser.
